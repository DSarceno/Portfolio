# {{ spec.name }} — Generated Project

> {{ spec.summary }}

This directory was generated by [`simgen`](../../README.md). It is self-contained
and depends only on `numpy`, `scipy`, `matplotlib` (and `manim` for the animation).

## Files

| File | Purpose |
|------|---------|
| `simulation.py` | Governing equations + numerical integration (`simulate()`). |
| `plot.py` | Renders the configured figures to PNG. |
| `scene.py` | Manim Community animation (`{{ class_name }}Scene`). |
| `report.tex` | LaTeX report; compile with `pdflatex report.tex`. |
| `spec.json` | The structured specification used to generate everything. |

## Quick start

```bash
# 1. Integrate the system and save the trajectory
python simulation.py

# 2. Render the figures
python plot.py

# 3. (optional) Render the animation
manim -pql scene.py {{ class_name }}Scene

# 4. (optional) Build the report
pdflatex report.tex
```

## Model summary

- **Domain:** {{ spec.domain }}
- **State variables ({{ spec.dimension }}):** {% for sv in spec.state_variables %}`{{ sv.symbol }}`{% if not loop.last %}, {% endif %}{% endfor %}

- **Integration method:** `{{ spec.method }}`
- **Time span:** `[{{ spec.t_start }}, {{ spec.t_end }}]` with `{{ spec.num_points }}` points

### Parameters

| Symbol | Value | Description |
|--------|-------|-------------|
{% for p in spec.parameters %}
| `{{ p.symbol }}` | {{ p.value }} | {{ p.description }} |
{% endfor %}
